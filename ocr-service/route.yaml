kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: ocr-service
  namespace: ocr-service
spec:
  host: ocr-service.apps.ocp.seatechit.com.vn
  to:
    kind: Service
    name: ocr-service
    weight: 100
  port:
    targetPort: 8000
  tls:
    termination: edge
apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: ocr-service
  labels:
    app: ocr-service
spec:
  selector:
    app: ocr-service
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP